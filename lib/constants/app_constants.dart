import 'package:flutter/material.dart';

class AppConstants {
  AppConstants._();

  static const double kSpacingUnit = 10;

  // Minimum button size
  static const Size kMinButtonSize =
      Size(kSpacingUnit * 4.6, kSpacingUnit * 4.6);

  // Default border radius
  static const double kDefaultBorderRadius = kSpacingUnit * 1.6;

  static const int kTimeOutDuration = 15;

  static const double kIconSize = 2.2;

  static const double kSideDrawerIconSize = 2.0;
}
